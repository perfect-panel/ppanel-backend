use crate::cache::Cache;
use crate::model::entity::user::UserSubscribe;

pub fn clear_trial_subscribe_cache(_cache: &Cache, _sub: &UserSubscribe) {
    // placeholder — cache invalidation logic goes here when needed
}
