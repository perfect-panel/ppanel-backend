[package]
name = "oauth"
version = "0.1.0"
edition = "2021"

[dependencies]
arctic-oauth = { version = "0.3.0", features = ["apple", "google"] }
base64 = "0.22"
hex = "0.4"
hmac = "0.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
