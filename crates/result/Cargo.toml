[package]
name = "result"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
axum = "0.8"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
